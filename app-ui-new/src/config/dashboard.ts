/**
 * Maximum age of dashboard tab data before it is considered stale
 * and automatically refreshed when the tab becomes active.
 *
 * Set to 0 to always refresh on tab switch.
 * Default: 60000 (1 minute)
 */
export const DASHBOARD_DATA_MAX_AGE_MS = 60_000
