.base-applinth-marketplace-ui: 
  variables:
    DIR: applinth-marketplace-ui
    IMAGE: ${REGISTRY}/${IMAGE_PREFIX}applinth-marketplace-ui
  
  rules:
    - if: '$FORCE_PIPELINE =~ /ALL/ || $FORCE_PIPELINE =~ /applinth-marketplace-ui/'
    - if: '$CI_COMMIT_BEFORE_SHA == $_BANNED_RUN_ALL_SHA'
      when: never
    - changes:
      - ${DIR}/**/*


applinth-marketplace-ui:build:
  stage: build
  needs: []
  extends:
    - .base-kaniko-build
    - .base-applinth-marketplace-ui
