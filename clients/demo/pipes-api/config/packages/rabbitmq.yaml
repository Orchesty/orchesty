# Repeater
rabbit_mq:
    connections:
        default:
            dsn: '%env(string:RABBIT_DSN)%'

    exchanges:
        repeater:
            durable: true
            type: direct

    queues:
        pipes.batch_connector:
            durable: true
