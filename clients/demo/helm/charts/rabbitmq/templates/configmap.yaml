---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rabbitmq.fullname" . }}
  labels:
{{ include "rabbitmq.labels" . | indent 4 }}
data:
  enabled_plugins: "[rabbitmq_consistent_hash_exchange,rabbitmq_management,rabbitmq_prometheus]."
