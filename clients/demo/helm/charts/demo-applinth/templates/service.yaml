apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-applinth.fullname" . }}
  labels:
    {{- include "demo-applinth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "demo-applinth.selectorLabels" . | nindent 4 }}
