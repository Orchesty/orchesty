---

## BEGIN DEV
version: '2'
## END DEV
## BEGIN STAGE
#version: '3.3'
## END STAGE

services:
    ## BEGIN DEV
    downloader:
        image: dkr.hanaboso.net/pipes/pipes/php-dev:dev
        user: ${DEV_UID}:${DEV_GID}
        environment:
          BACKEND_HOST: ${BACKEND_URL}/
          ELASTIC_HOST: elasticsearch
          ELASTIC_INDEX: 'index'
        command: bash -c 'while ! [ -f vendor/autoload.php ];do sleep 1; echo "sleep 1"; done; bin/console downloader:run'
        working_dir: /var/www/pipes/clients/demo/pipes-api
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ${HOME}/.composer:/srv/.composer
    ## END DEV
    ## BEGIN STAGE
    #donwlader:
    #    image: dkr.hanaboso.net/pipes/demo/monolith:${TAG_MONOLITH}
    #    command: bin/console downloader:run
    #    environment:
    #      BACKEND_HOST: ${BACKEND_URL}/
    #      ELASTIC_HOST: elasticsearch
    #      ELASTIC_INDEX: 'logstash-2018.01.31'
    ## END STAGE

    ## BEGIN DEV
    batch:
        image: dkr.hanaboso.net/pipes/pipes/php-dev:dev
        user: ${DEV_UID}:${DEV_GID}
        environment:
          BACKEND_HOST: ${BACKEND_URL}/
          ELASTIC_HOST: elasticsearch
          ELASTIC_INDEX: 'index'
        command: bash -c 'while ! [ -f vendor/autoload.php ];do sleep 1; echo "sleep 1"; done; bin/console rabbit-mq:async-consumer batch'
        working_dir: /var/www/pipes/clients/demo/pipes-api
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ${HOME}/.composer:/srv/.composer
    ## END DEV
    ## BEGIN STAGE
    #batch:
    #    image: dkr.hanaboso.net/pipes/demo/monolith:${TAG_MONOLITH}
    #    command: bin/console rabbit-mq:async-consumer batch
    #    environment:
    #      BACKEND_HOST: ${BACKEND_URL}/
    #      ELASTIC_HOST: elasticsearch
    #      ELASTIC_INDEX: 'logstash-2018.01.31'
    #      PHP_FPM_MAX_REQUESTS: 5000
    ## END STAGE

    ## BEGIN DEV
    long-running-node:
        image: dkr.hanaboso.net/pipes/pipes/php-dev:dev
        user: ${DEV_UID}:${DEV_GID}
        command: bash -c 'while ! [ -f vendor/autoload.php ];do sleep 1; echo "sleep 1"; done; bin/console rabbit-mq:consumer long-running-node'
        working_dir: /var/www/pipes/clients/demo/pipes-api
        environment:
          BACKEND_HOST: ${BACKEND_URL}/
          ELASTIC_HOST: elasticsearch
          ELASTIC_INDEX: 'logstash-2018.01.31'
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ${HOME}/.composer:/srv/.composer
    ## END DEV
    ## BEGIN STAGE
    #long-running-node:
    #    image: dkr.hanaboso.net/pipes/pipes/php-dev:${TAG_MONOLITH}
    #    command: bin/console rabbit-mq:consumer long-running-node
    #    environment:
    #      BACKEND_HOST: ${BACKEND_URL}/
    #      ELASTIC_HOST: elasticsearch
    #      ELASTIC_INDEX: 'logstash-2018.01.31'
    #      PHP_FPM_MAX_REQUESTS: 5000
    ## END STAGE

    ## BEGIN DEV
    status-service:
        image: dkr.hanaboso.net/pipes/pipes/php-dev:dev
        user: ${DEV_UID}:${DEV_GID}
        environment:
          BACKEND_HOST: ${BACKEND_URL}/
          ELASTIC_HOST: elasticsearch
          ELASTIC_INDEX: 'index'
        command: bash -c 'while ! [ -f vendor/autoload.php ];do sleep 1; echo "sleep 1"; done; bin/console rabbit-mq:consumer status-service'
        working_dir: /var/www/pipes/clients/demo/pipes-api
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ${HOME}/.composer:/srv/.composer
    ## END DEV
    ## BEGIN STAGE
    #status-service:
    #    image: dkr.hanaboso.net/pipes/demo/monolith:${TAG_MONOLITH}
    #    command: bin/console rabbit-mq:consumer status-service
    #    environment:
    #      BACKEND_HOST: ${BACKEND_URL}/
    #      ELASTIC_HOST: elasticsearch
    #      ELASTIC_INDEX: 'logstash-2018.01.31'
    #      PHP_FPM_MAX_REQUESTS: 5000
    ## END STAGE

    multi-probe:
      image: 'dkr.hanaboso.net/pipes/pipes/multi-probe:dev'
      environment:
        REDIS_HOST: 'redis'
        REDIS_PORT: '6379'
        REDIS_PASS: ''
        REDIS_DB: '0'
      ports:
        - ${DEV_IP}:8007:8007

    multi-counter:
      image: 'dkr.hanaboso.net/pipes/pipes/pf-bridge:dev'
      user: ${DEV_UID}:${DEV_GID}
      environment:
        # NODE_ENV: debug
        RABBITMQ_HOST: rabbitmq
        RABBITMQ_PORT: '5672'
        RABBITMQ_USER: guest
        RABBITMQ_PASS: guest
        RABBITMQ_VHOST: /
        REDIS_HOST: 'redis'
        REDIS_PORT: '6379'
        REDIS_PASS: ''
        REDIS_DB: '0'
        COUNTER_PREFETCH: '1'
        METRICS_HOST: 'kapacitor'
        METRICS_PORT: '9100'
      command: './dist/src/bin/pipes.js start multi_counter'
      ports:
        - ${DEV_IP}:8005:8005
      # TODO: unify usages and remove the alias
      networks:
        default:
          aliases:
            - multi-counter-api

    ## BEGIN DEV
    starting-point:
      image: dkr.hanaboso.net/pipes/pipes/starting-point:app-dev
      environment:
        APP_DEBUG: "false"
        GO111MODULE: "on"
        MONGO_HOSTNAME: mongo
        MONGO_DATABASE: demo
        RABBIT_COUNTER_QUEUE_DURABLE: "true"
        RABBIT_QUEUE_DURABLE: "true"
        RABBIT_DELIVERY_MODE: 2
        APP_CLEANUP_TIME: "300"
      working_dir: /go/src/hanaboso/starting-point
      volumes:
        - ./../../go/src/hanaboso/starting-point/:/go/src/hanaboso/starting-point
      ports:
        - ${DEV_IP}:8000:80
      command: go run cmd/starting-point.go
    ## END DEV
    ## BEGIN STAGE
    #starting-point:
    #  image: dkr.hanaboso.net/pipes/pipes/starting-point:dev
    #  environment:
    #    APP_DEBUG: "false"
    #    MONGO_HOSTNAME: mongo
    #    MONGO_DATABASE: demo
    #    RABBIT_COUNTER_QUEUE_DURABLE: "true"
    #    RABBIT_QUEUE_DURABLE: "true"
    #    RABBIT_DELIVERY_MODE: 2
    #    APP_CLEANUP_TIME: "300"
    ## END STAGE

    repeater:
      image: 'dkr.hanaboso.net/pipes/pipes/pf-bridge:dev'
      user: ${DEV_UID}:${DEV_GID}
      command: './dist/src/bin/pipes.js start repeater'
      environment:
        RABBITMQ_HOST: rabbitmq
        RABBITMQ_PORT: '5672'
        RABBITMQ_USER: guest
        RABBITMQ_PASS: guest
        RABBITMQ_VHOST: /
        MONGO_HOST: mongo
        REPEATER_CHECK_TIMEOUT: '5000'

    # PHP Services monolith
    ## BEGIN DEV
    monolith-api:
        image: dkr.hanaboso.net/pipes/pipes/php-dev:dev
        user: ${DEV_UID}:${DEV_GID}
        environment:
          PHP_WEBROOT: /var/www/pipes/clients/demo/pipes-api/public
          BACKEND_HOST: ${BACKEND_URL}/
          ELASTIC_HOST: elasticsearch
          ELASTIC_INDEX: 'index'
        working_dir: /var/www/pipes/clients/demo/pipes-api
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ${HOME}/.composer:/srv/.composer
    ## END DEV
    ## BEGIN STAGE
    #monolith-api:
    #    image: dkr.hanaboso.net/pipes/demo/monolith:${TAG_MONOLITH}
    #    environment:
    #      BACKEND_HOST: ${BACKEND_URL}/
    #      ELASTIC_HOST: elasticsearch
    #      ELASTIC_INDEX: 'logstash-2018.01.31'
    #    secrets:
    #      - source: clevermonitor-ca.pem
    #        target: /var/www/html/var/secret/ca.crt
    #      - source: clevermonitor-hanaboso.pem
    #        target: /var/www/html/var/secret/keypair.pem
    ## END STAGE

    ## BEGIN DEV
    topology-api:
        user: ${DEV_UID}:${DEV_GID}
        image: dkr.hanaboso.net/pipes/pipes/topology-api:dev
        environment:
          DEPLOYMENT_PREFIX: demo
          GENERATOR_NETWORK: demo_default
          GENERATOR_MODE: compose
          GENERATOR_PATH: /srv/topology
          MONGO_HOST: mongo
          MONGO_DATABASE: demo
          RABBITMQ_HOST: rabbitmq
          PROJECT_SOURCE_PATH: ${PROJECT_SOURCE_PATH}/../topology
        volumes:
          - ${DOCKER_SOCKET_PATH}:/var/run/docker.sock
          - ../topology:/srv/topology
    ## END DEV
    ## BEGIN STAGE
    #topology-api:
    #    image: dkr.hanaboso.net/pipes/pipes/topology-api:dev
    #    environment:
    #      DEPLOYMENT_PREFIX: ${DEPLOYMENT_PREFIX}
    #      DOCKER_HOST: tcp://${DOCKER_HOST}
    #      DOCKER_CERT_PATH: /srv/docker-pki
    #      DOCKER_TLS_VERIFY: 1
    #      GENERATOR_NETWORK: ${DEPLOYMENT_PREFIX}_default
    #      GENERATOR_MODE: swarm
    #      GENERATOR_PATH: /tmp
    #      MONGO_HOST: mongo
    #      MONGO_DATABASE: demo
    #      RABBITMQ_HOST: rabbitmq
    #    secrets:
    #      - source: docker-ca.pem
    #        target: /srv/docker-pki/ca.pem
    #      - source: docker-client-cert.pem
    #        target: /srv/docker-pki/cert.pem
    #      - source: docker-client-key.pem
    #        target: /srv/docker-pki/key.pem
    #      - source: docker-config.json
    #        target: /root/.docker/config.json
    ## END STAGE

    frontend:
    ## BEGIN DEV
        image: dkr.hanaboso.net/pipes/pipes/nodejs-build:dev
        user: ${DEV_UID}:${DEV_GID}
        working_dir: /var/www/pipes/frontend
        environment:
          PHP_WEBROOT: /var/www/pipes/clients/demo/pipes-api/public
          NPM_INSTALL: ${NPM_INSTALL}
          NPM_START: "yes" # webpackserver
        ports:
          - ${DEV_IP}:80:80
          - ${DEV_IP}:81:8080
        volumes:
          - ../../:/var/www/pipes
          - ${SSH_AUTH_SOCK}:/tmp/bound-ssh-agent
          - ./stack/nginx.conf:/etc/nginx/nginx.conf
        # TODO: Use dynamic dns resolution in Nginx instead
        depends_on:
          - monolith-api
          - topology-api
          - notification-sender-fpm
    ## END DEV
    ## BEGIN STAGE
    #    image: dkr.hanaboso.net/pipes/pipes/frontend:${TAG_FRONTEND}
    #    environment:
    #      BACKEND_URL: ${BACKEND_URL}
    #      PHP_WEBROOT: /var/www/html/public
    #    ports:
    #      - ${PUBLISH_HTTP_PORT}:80
    ## END STAGE
    # TODO: check if we can move some of these aliases directly to monolith-api
        networks:
          default:
            aliases:
              - connector-api
              - mailer-api
              - ftp-api
              - mapper-api
              - notification-sender-api

    stream:
        image: dkr.hanaboso.net/pipes/pipes/stream:dev
        environment:
          - RABBITMQ_HOST=rabbitmq
          - STREAM_WS_PORT=80
          - STREAM_HTTP_PORT=3030
          - STREAM_QUEUE=pipes.stream
        ports:
          - ${DEV_IP}:3030:3030
        # - ${DEV_IP}:8080:80

    # RabbitMQ
    rabbitmq:
        image: rabbitmq:3-management-alpine
        ports:
          - ${DEV_IP}:15672:15672
        volumes:
          - rabbitmq:/var/lib/rabbitmq
    ## BEGIN STAGE
    #    deploy:
    #      resources:
    #        limits:
    #          memory: 1G
    ## END STAGE

    # Storage
    mongo:
        image: mongo:3.4
        ports:
          - ${DEV_IP}:27017:27017
        volumes:
          - mongodb:/data/db
    ## BEGIN STAGE
    #    deploy:
    #      resources:
    #        limits:
    #          memory: 2G
    ## END STAGE

    redis:
        image: redis:3.2-alpine
        ports:
        - ${DEV_IP}:6379:6379
        volumes:
        - redis:/data

    logstash:
        image: dkr.hanaboso.net/pipes/pipes/logstash:dev
        environment:
          - MONGO_HOST=mongo
          - MONGO_DATABASE=demo
          - MONGO_COLLECTION=Logs
          - LS_JAVA_OPTS=-Xms512m -Xmx512m
        #command: --log.level debug
        ## BEGIN DEV
        volumes:
          - ./stack/logstash.conf:/usr/share/logstash/pipeline/logstash.conf
        ## END DEV
        ## BEGIN STAGE
        #restart: on-failure
        #configs:
        #  - source: logstash.conf
        #    target: /usr/share/logstash/pipeline/logstash.conf
        ## END STAGE

    # Metrics
    influxdb:
        image: dkr.hanaboso.net/pipes/pipes/influxdb:dev
        environment:
          - INFLUXDB_DB=pipes
        ports:
          - ${DEV_IP}:8083:8083
          - ${DEV_IP}:8086:8086
          - ${DEV_IP}:8089:8089/udp
        volumes:
          - influxdb:/var/lib/influxdb

    telegraf:
        image: telegraf:1.5-alpine
        ## BEGIN DEV
        volumes:
          - ./stack/telegraf.conf:/etc/telegraf/telegraf.conf
        ## END DEV
        ## BEGIN STAGE
        #configs:
        #  - source: telegraf.conf
        #    target: /etc/telegraf/telegraf.conf
        ## END STAGE

    kapacitor:
        image: dkr.hanaboso.net/pipes/pipes/kapacitor:dev
        hostname: kapacitor

    notification-sender-fpm:
        image: dkr.hanaboso.net/clever-monitor/clever-notification-sender:master
        ## BEGIN DEV
        environment:
            - DEV_UID=${DEV_UID}
            - DEV_GID=${DEV_GID}
            - BACKEND_HOST=${BACKEND_URL}
        ## END DEV
        ## BEGIN STAGE
        #environment:
        #    - BACKEND_HOST=${BACKEND_URL}
        ## END STAGE

    cron-api:
        image: dkr.hanaboso.net/pipes/pipes/python-cron:dev
        ## BEGIN DEV
        user: ${DEV_UID}:${DEV_GID}
        environment:
          - DEV_UID={DEV_UID}
          - DEV_GID={DEV_GID}
        ## END DEV
        ports:
          - ${DEV_IP}:5000:5000
        links:
          - mongo

## BEGIN DEV
volumes:
  mongodb: {}
  rabbitmq: {}
  redis: {}
  influxdb: {}
## END DEV

## BEGIN STAGE
#volumes:
#  influxdb:
#    driver: local
#    driver_opts:
#      type: none
#      device: /srv/persistent-data/${DEPLOYMENT_PREFIX}/influxdb
#      o: bind
#  mongodb:
#    driver: local
#    driver_opts:
#      type: none
#      device: /srv/persistent-data/${DEPLOYMENT_PREFIX}/mongodb
#      o: bind
#  rabbitmq:
#    driver: local
#    driver_opts:
#      type: none
#      device: /srv/persistent-data/${DEPLOYMENT_PREFIX}/rabbitmq
#      o: bind
#  redis:
#    driver: local
#    driver_opts:
#      type: none
#      device: /srv/persistent-data/${DEPLOYMENT_PREFIX}/redis
#      o: bind
#
#configs:
#  logstash.conf:
#    file: ./stack/logstash.conf
#  telegraf.conf:
#    file: ./stack/telegraf.conf
#
#secrets:
#  stage-staff.pem:
#    external: true
#  clevermonitor-ca.pem:
#    external: true
#  clevermonitor-hanaboso.pem:
#    external: true
#  docker-ca.pem:
#    external: true
#  docker-client-cert.pem:
#    external: true
#  docker-client-key.pem:
#    external: true
#  docker-config.json:
#    external: true
#
#networks:
#  default:
#    driver: overlay
#    ipam:
#      config:
#        - subnet: 10.75.0.0/16
## END STAGE
