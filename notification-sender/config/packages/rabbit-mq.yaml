rabbit_mq:
    logger: 'monolog.logger.rabbit-mq'

    connections:
        default:
            dsn: '%env(string:RABBITMQ_DSN)%'

    queues:
        notification-rabbit:
        notification:

    publishers:
        notification-rabbit:

    consumers:
        notification:
            queue: notification
            callback: notification.callback.message
