---
version: '3'

services:
    topology-generator:
        image: golang:latest
        working_dir: /go/src/hanaboso/topologygenerator
        volumes:
          - ../:/go/src/hanaboso/
        command: bash -c 'sleep infinity'