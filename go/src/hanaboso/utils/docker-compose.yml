---
version: '3'

services:
    utils:
        image: golang:latest
        working_dir: /go/src/hanaboso/utils
        volumes:
          - ../:/go/src/hanaboso/
        command: bash -c 'sleep infinity'