dbrp "pipes"."1m"

var sample = 30m
var retention = '30m'

batch
   |query('SELECT mean("avg.time") as "avg.time",  min("min.time") as "min.time", max("max.time") as "max.time" FROM "pipes"."1m"."connectors"')
       .period(sample)
       .every(sample)
       .groupBy('topology_id', 'node_id')
   |influxDBOut()
       .database('pipes')
       .measurement('connectors')
       .precision('ms')
       .retentionPolicy(retention)