{
  "name": "orchesty",
  "scripts": {
    "prepare-husky": "husky install .husky",
    "pre-commit": "lint-staged --concurrent false"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.3"
  }
}
