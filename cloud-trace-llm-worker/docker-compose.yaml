services:
    sdk:
        image: node-sdk:tag
        build: ./docker
        user: ${DEV_UID}:${DEV_GID}
        working_dir: /var/www
        volumes:
            - ./:/var/www
        environment:
            APP_ENV: 'debug'
        command: /bin/sh -c 'pnpm i && pnpm run start'
