framework:
    secret: "%secret%"
    csrf_protection:
        enabled: false
    default_locale: "%locale%"
    trusted_hosts: ~
    fragments: ~
    http_method_override: true
    test: false
    serializer:
    session:
        storage_id: session.storage.mock_file
    router:
        strict_requirements: ~

parameters:
    starting_point_host: 'https://starting-point'
    backend_host: 'https://127.0.0.11'

    # Base params
    locale: en

    # A secret key that's used to generate certain security-related tokens
    secret: ThisTokenIsNotSoSecretChangeIt