meta {
  name: Login
  seq: 1
}

post {
  url: http://{{backendUrl}}/api/user/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "test@orchesty.io",
    "password": "password"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
